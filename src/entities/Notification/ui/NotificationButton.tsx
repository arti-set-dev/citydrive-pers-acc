import { Button } from '@/shared/ui/Button/Button';
import NotificationIcon from '@/shared/assets/icons/bell.svg';

export const NotificationButton = () => {
  return <Button variant="clear" icon={NotificationIcon} />;
};
