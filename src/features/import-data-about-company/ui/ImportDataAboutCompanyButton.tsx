import { Button } from '@/shared/ui/Button/Button';

export const ImportDataAboutCompanyButton = () => {
  return (
    <Button variant="clear" offset={0}>
      Импортировать базу
    </Button>
  );
};
